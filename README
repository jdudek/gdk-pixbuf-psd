Installation instructions

$ tar zxvf gdk-pixbuf-psd_20081217.tar.gz
$ cd gdk-pixbuf-psd_20081217
$ make
# cp libpixbufloader-psd.so /usr/lib/gdk-pixbuf-2.0/2.10.0/loaders/

Now you need to setup loader with gdk-pixbuf-query-loaders:

# gdk-pixbuf-query-loaders --update-cache

Enjoy!